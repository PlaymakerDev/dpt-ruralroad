import React from 'react'

const Header = (props) => {
  const { } = props

  return (
    <nav className='p-5'>
      <h1 className='text-white'>Sample</h1>
    </nav>
  )
}

export default React.memo(Header)
